export interface IValues {
  name: string
  surname: string
  email: string
  phone: number
}

export interface IProps {
  id: number,
  title: string,
  text: string | null,
  buttonText: string | null,
  image: HTMLImageElement,
} 
